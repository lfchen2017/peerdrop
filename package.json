{
  "name": "peerdrop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step'",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
